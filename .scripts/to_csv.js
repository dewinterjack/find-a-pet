// Create a cursor of the animals collection, rename the _id column to Animal_ID
// Export this to csv with this command: mongo meteor to_csv.js > animals.csv

// Convert this to my case.

// print("name,id,email");
// db.User.find().forEach(function(user){
//   print(user.name+","+user._id.valueOf()+","+user.email);
// });
